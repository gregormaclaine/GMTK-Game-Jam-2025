const CREDITS = `
    I am the credits :)
`.split('\n');

const PAUSE_KEY_CODE = 27;
const DEV_LOG_HITBOX_KEY_CODE = 76; // 'L' key for development logging
const DEV_BOUNDARY_CREATE_KEY_CODE = 75; // 'K' key for boundary creation mode

const SHOW_HITBOXES = false;

const SCREEN_SCALE = 2;
